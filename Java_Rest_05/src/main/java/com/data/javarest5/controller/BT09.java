package com.data.javarest5.controller;

public class BT09 {
//    Sử dụng Spring Cache hoặc Redis Cache để lưu kết quả các truy vấn tĩnh hoặc thường xuyên sử dụng
//Trả về DTO thay vì Entity (chỉ chứa trường cần thiết).
//Thêm chỉ mục (index) vào các trường được lọc nhiều (ví dụ: username, email).
//
//    Sử dụng @Query hoặc JPQL với điều kiện rõ ràng.
//
//    Tránh N+1 Query Problem (sử dụng @EntityGraph hoặc JOIN FETCH).
}
